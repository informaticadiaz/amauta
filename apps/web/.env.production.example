# =============================================================================
# Amauta Web - Production Environment Variables
# =============================================================================
#
# ⚠️ IMPORTANTE - SEGURIDAD:
# Este es un archivo de EJEMPLO que SÍ se sube al repositorio público.
# Los valores reales deben estar en .env.production.local (NO se sube al repo)
#
# Para deployment en producción:
# 1. Copiar este archivo: cp .env.production.example .env.production.local
# 2. Reemplazar todos los valores con datos REALES
# 3. NUNCA commitear .env.production.local (está en .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# General
# -----------------------------------------------------------------------------
NODE_ENV=production

# -----------------------------------------------------------------------------
# Next.js
# -----------------------------------------------------------------------------
# Puerto interno (Dokploy lo mapea automáticamente)
PORT=3000

# -----------------------------------------------------------------------------
# API Backend
# -----------------------------------------------------------------------------
# URL del backend (servidor - solo accesible en server components)
API_URL=https://api.tu-dominio.com

# URL del backend (cliente - accesible en browser)
NEXT_PUBLIC_API_URL=https://api.tu-dominio.com

# -----------------------------------------------------------------------------
# NextAuth.js
# -----------------------------------------------------------------------------
# URL pública del frontend
NEXTAUTH_URL=https://tu-dominio.com

# ⚠️ CRÍTICO: Mismo secret que en el backend
# Comando: openssl rand -base64 32
NEXTAUTH_SECRET=CAMBIAR-mismo-secret-que-en-backend

# -----------------------------------------------------------------------------
# App Info (público - va al cliente)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_NAME=Amauta
NEXT_PUBLIC_APP_URL=https://tu-dominio.com
NEXT_PUBLIC_APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# Analytics (OPCIONAL)
# -----------------------------------------------------------------------------
# Google Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# Error Tracking (OPCIONAL)
# -----------------------------------------------------------------------------
# Sentry DSN (público - va al cliente)
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/xxx

# -----------------------------------------------------------------------------
# PWA
# -----------------------------------------------------------------------------
NEXT_PUBLIC_PWA_ENABLED=true

# -----------------------------------------------------------------------------
# Feature Flags (público - va al cliente)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_ENABLE_OFFLINE_MODE=true
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# -----------------------------------------------------------------------------
# External Services (OPCIONAL)
# -----------------------------------------------------------------------------
# Cloudinary para imágenes (público)
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=tu-cloud-name

# AWS S3 para storage (servidor)
# AWS_ACCESS_KEY_ID=CAMBIAR-tu-access-key
# AWS_SECRET_ACCESS_KEY=CAMBIAR-tu-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=amauta-uploads

# -----------------------------------------------------------------------------
# Recaptcha (público - va al cliente)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_RECAPTCHA_SITE_KEY=tu-site-key
# RECAPTCHA_SECRET_KEY=CAMBIAR-tu-secret-key

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# Variables NEXT_PUBLIC_*:
# - Son accesibles en el CLIENTE (browser)
# - Se incluyen en el bundle de JavaScript
# - NO usar para secrets o información sensible
#
# Variables sin NEXT_PUBLIC_:
# - Solo accesibles en el SERVIDOR
# - Para secrets, API keys, conexiones
# - Más seguras
#
# En Dokploy:
# 1. Ir a la aplicación → Environment Variables
# 2. Pegar las variables con valores REALES
# 3. Rebuild para que Next.js tome las nuevas variables
#
# Build time vs Runtime:
# - NEXT_PUBLIC_* se evalúan en BUILD time
# - Variables server-only se evalúan en RUNTIME
# - Si cambias NEXT_PUBLIC_*, necesitas rebuild
#
# Rotación de secrets:
# - NEXTAUTH_SECRET cada 3-6 meses (coordinar con backend)
# - AWS keys cada 6-12 meses
# - Documentar en changelog privado
# =============================================================================
