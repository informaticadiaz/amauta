// @amauta/types - Definiciones de tipos compartidos

// Este archivo se expandirá cuando tengamos modelos reales
// Por ahora es un placeholder para validar la estructura del monorepo

export interface User {
  id: string;
  email: string;
  name: string;
}

export interface Course {
  id: string;
  title: string;
  description: string;
}
